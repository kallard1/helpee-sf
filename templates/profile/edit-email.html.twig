{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h2 class="title">{{ 'profile.edit_email'|trans }}</h2>
        {{ form_start(form) }}
        <div class="form-group col-12">
            {{ form_label(form.email, 'profile.label.email') }}
            {{ form_widget(form.email) }}
        </div>

        <div class="form-group col-12 btn-container">
            <button type="submit" class="btn btn-fit btn-submit col-12 active">{{ 'profile.email.button.submit'|trans }}</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}

